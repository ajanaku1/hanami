[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
remappings = [
  "@openzeppelin/=lib/openzeppelin-contracts/",
  "forge-std/=lib/openzeppelin-contracts/lib/forge-std/src/",
]

[rpc_endpoints]
zerog = "${OG_RPC_URL}"

[etherscan]
zerog = { key = "${CHAINSCAN_API_KEY}", url = "${CHAINSCAN_API_URL}", chain = 16661 }
